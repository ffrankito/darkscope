interface:
  display_name: "DarkScope"
  short_description: "Pentest enterprise por niveles"
  default_prompt: "Use $darkscope to run an authorized security review with the right depth level and a clean final report."
